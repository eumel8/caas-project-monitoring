categories:
  - monitoring
questions:
  - variable: hiller.grafana.enabled
    default: "true"
    description: Enable grafana
    type: boolean
    label: grafana Enabled
    group: grafana
    show_subquestion_if: true
    subquestions:
      - variable: hiller.grafana.defaultDashboardsTimezone
        description: grafana dashboard timezone
        type: string
        label: grafana dashboard timezone
      - variable: hiller.grafana.repository
        description: Repository of grafana Image
        type: string
        label: grafana Image Repo
        group: grafana
      - variable: hiller.grafana.tag
        description: Tag of grafana Image
        type: string
        label: grafana Image Tag
        group: grafana
        group: grafana
  - variable: ha.b.enabled
    default: "true"
    description: Enable HA
    type: boolean
    label: ha enabled
    group: demoapp
    show_subquestion_if: true
    subquestions:
      - variable: ha.b.replicaCount
        description: Replica Count of PODs
        type: string
        label: ha replicacount
        group: demoapp
        default: 3
